# 케빈샘 인스타 링크 페이지

인스타 릴스를 보고 넘어온 사람을 **메이커 광장으로 보내는** 단일 랜딩 페이지.

성공 기준은 **광장 클릭**이다. 문의 클릭 수가 아니다.
방문자 대부분은 아직 문의할 마음이 없는 상태로 들어온다.

---

## 문서

| 파일 | 언제 읽나 |
|---|---|
| `BRIEF.md` | **작업 시작 전 반드시.** 기획 원본, 판단 기준 |
| `CONTRACT.md` | API·디자인 토큰·상수를 다룰 때 |
| `README.md` | 작업 분담과 순서를 확인할 때 |

지시서끼리 충돌하면 **`BRIEF.md`를 따르고 사람에게 보고한다.**

---

## 절대 규칙

### 1. 섹션을 추가하지 않는다

아래는 의도적으로 뺀 것이다. 페이지를 "완성도 있게" 만들려고 붙이지 말 것.

> About / 원장 인사말 / 커리큘럼 / 강사진 / 수강료 / FAQ / 후기 목록 / 푸터 / 스크롤 유도

전부 학원 홈페이지 몫이다. 이 페이지가 길어지면 클릭이 줄어든다.

### 2. 카피를 수정하지 않는다

```
여기 있는 건 전부 아이들이 만들었어요
공부한 결과가 아니라, 놀다가 만든 것들
```

"더 자연스럽게" 다듬지 말 것. 의도적으로 뾰족하게 잡은 문구다.
입시 목적 학부모가 걸러지는 것은 실패가 아니라 설계다.

"창의력", "자기주도 학습" 같은 학원 자랑 문구를 추가하지 않는다.
**작품이 스스로 말하게 두고, 문구는 안내만 한다.**

### 3. 모르는 값을 지어내지 않는다

전화번호·주소·URL을 그럴듯하게 채우지 말 것.
`CONTRACT.md` 1절의 `TODO_` 상수 그대로 두고 보고한다.

### 4. `100vh` 금지, `100dvh` 사용

인스타 인앱 브라우저의 하단 UI가 화면을 가려서 `100vh`를 쓰면 버튼이 잘린다.
유입은 사실상 100% 인앱 브라우저다.

### 5. 광장 카드가 주인공이다

화면에서 가장 크고 눈에 띄어야 한다.
문의나 학원 페이지 버튼이 더 강해지면 페이지가 목적을 잃는다.

### 6. 범위를 넘지 않는다

개선 아이디어는 구현하지 말고 리포트에 적는다.
막히면 추측해서 진행하지 말고 멈추고 보고한다.

---

## 파일 소유권

자기 소유가 아닌 경로를 수정하지 않는다.

| 경로 | 소유 |
|---|---|
| `/link/**` | link-frontend |
| `/api/plaza/**`, 썸네일 파이프라인 | plaza-api |
| 광장 작품 상세·업로드, 작품 스키마 | plaza-mobile |

경계 주의:
- `mobileSupported` 필드 — 정의는 plaza-mobile, 소비는 plaza-api
- 썸네일 리사이즈 — plaza-api 소유
- 디자인 토큰 — 광장에 CSS 변수가 있으면 그쪽이 원본. 복제하지 말고 참조

---

## 서브에이전트

`.claude/agents/` 에 정의되어 있다.

| 이름 | 역할 |
|---|---|
| `link-frontend` | `/link` 페이지 구현 |
| `plaza-api` | 최신 작품 API + 썸네일 |
| `plaza-mobile` | PC 전용 안내 배너, 모바일 지원 플래그 |
| `link-review` | 검수 (읽기 전용) |

**병렬로 돌린다.** `link-frontend`는 `plaza-api`를 기다리지 않고
`CONTRACT.md` 스키마에 맞춘 `/link/mock/recent.json` 으로 개발한 뒤,
나중에 엔드포인트만 교체한다.

---

## 우선순위

시간이 부족하면 이 순서로 자른다.

1. `link-frontend` + `plaza-api` — 없으면 페이지가 없다
2. `plaza-mobile`의 PC 전용 배너 — 없으면 최악의 사용자 경험이 발생한다
3. `plaza-mobile`의 모바일 플래그 — 배너가 대신 막아준다
4. `link-review` — 사람이 대신할 수 있다

플래그가 늦어지면 `plaza-api`는 `mobile` 파라미터를 무시하고 전체를 반환한다.

---

## 미해결

- **성인반 노출 여부** — 광장에 `성인` 카테고리가 있으나 링크 페이지는 `초등~고등`으로 잡았다.
  헤드라인이 "아이들이 만들었어요"라 톤이 어긋난다. 사람의 결정 전까지 현재 값 유지.
